name: Build and Deploy to GitHub Pages
on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Unzip the starter
        run: |
          sudo apt-get update && sudo apt-get install -y unzip
          unzip -o soap-calc-pro-starter.zip

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm ci
      # Force the correct base path for GitHub Pages
      - run: npm run build -- --base=/belleis-soap-calculator/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
